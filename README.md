# 🎯 VORTEX IP Hunter

> **Автоматизированная система поиска Floating IP с защитой от передачи**

## ⚡ Быстрая установка

```bash
export VORTEX_SERVER='http://178.250.247.165:5000'
curl -sL https://raw.githubusercontent.com/Mastachok/VORTEX_HUNTER/main/install.sh | bash
```

## 📋 Процесс активации

### 1. Получите доступ
```
1. Откройте Telegram → @vortex_hunter_bot
2. Отправьте /start
3. Дождитесь одобрения администратора
4. Получите код активации и инструкции
```

### 2. Установите на ВМ
```bash
export VORTEX_SERVER='http://178.250.247.165:5000'
curl -sL https://raw.githubusercontent.com/Mastachok/VORTEX_HUNTER/main/install.sh | bash
```

### 3. Активируйте
```bash
vortex activate
# Введите код из Telegram бота
```

### 4. Настройте через бота
```
Telegram → @vortex_hunter_bot
[⚙️ Настройки] → Добавьте аккаунты и подсети
```

### 5. Запустите
```bash
vortex start
# или
systemctl start vortex-hunt
```

## 🎮 Команды

```bash
vortex activate   # Активация системы
vortex start      # Запуск охоты
vortex stop       # Остановка
vortex status     # Статус и статистика
```

### Фоновый режим
```bash
systemctl start vortex-hunt     # Запуск в фоне
systemctl status vortex-hunt    # Статус
systemctl enable vortex-hunt    # Автозапуск при загрузке

# Логи
tail -f /opt/vortex/logs/hunt.log
journalctl -u vortex-hunt -f
```

## 📊 Как работает

### Архитектура
```
┌─────────────────┐          ┌─────────────────┐
│  Telegram Bot   │◄────────►│  Пользователь   │
│  (Сервер)       │          │  (@telegram)    │
└────────┬────────┘          └─────────────────┘
         │
         │ API (активация, конфиг)
         │
         ▼
┌─────────────────┐
│  Клиент (ВМ)    │
│  - Охота IP     │
│  - Статистика   │
└─────────────────┘
```

### Процесс охоты
1. Клиент запрашивает конфиг с сервера (аккаунты + подсети)
2. Параллельная охота на всех аккаунтах
3. Проверка каждого нового FIP на соответствие подсетям
4. При совпадении → привязка к серверу
5. Статистика сохраняется локально

## 🔐 Безопасность

### Что защищает систему:

- **HWID привязка** - нельзя перенести на другую машину
- **IP привязка** - нельзя переместить ВМ
- **Telegram ID** - только владелец управляет
- **Server-side config** - пароли не хранятся на клиенте
- **Система одобрения** - администратор контролирует доступ
- **Heartbeat** - сервер может отключить доступ удалённо

### Почему нельзя передать:

❌ Скачать код с GitHub → Нужна активация через бота  
❌ Получить код активации → Нужно одобрение администратора  
❌ Активировать на другой ВМ → HWID + IP не совпадут  
❌ Обойти проверку → Конфиг на сервере, локально не хранится  

**Без доступа к серверу система НЕ РАБОТАЕТ**

## 🗂️ Структура файлов

```
/opt/vortex/
├── vortex_client.py          # Основной скрипт
├── install.sh                # Установщик
├── .vortex_activation        # Код активации (не трогать!)
├── .vortex_config.json       # Кэш конфига
├── .vortex_running           # Флаг охоты
├── vortex_hunt.db            # БД статистики
└── logs/
    ├── hunt_YYYYMMDD.log     # Логи охоты
    └── hunt_error.log        # Ошибки
```

## 📈 Статистика

```bash
vortex status

# Показывает:
# - Статус активации ✅/❌
# - Охота работает/остановлена 🟢/🔴
# - Количество аккаунтов 👥
# - Количество подсетей 🎯
# - Статистика захватов по аккаунтам 📊
# - Последние захваты 📝
```

## 🐛 Отладка

### Не удалось подключиться к серверу
```bash
# Проверьте переменную
echo $VORTEX_SERVER

# Проверьте доступность
curl http://178.250.247.165:5000/api/heartbeat

# Переустановите с правильным адресом
export VORTEX_SERVER='http://178.250.247.165:5000'
bash install.sh
```

### Доступ запрещён
- HWID или IP изменились
- Лицензия заблокирована администратором
- Свяжитесь с администратором через бота

### openstacksdk не установлен
```bash
cd /opt/vortex
source venv/bin/activate
pip install openstacksdk
```

## 📞 Поддержка

При проблемах обращайтесь к администратору через Telegram бот.

## ⚠️ Правовая информация

Проприетарное ПО. Передача третьим лицам запрещена.

---

**VORTEX IP Hunter** - Защищённая система для профессионалов 🎯
